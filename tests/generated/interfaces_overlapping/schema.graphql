interface Node {
    id: ID!
}

interface Named {
    name: String!
}

type User implements Node & Named {
    id: ID!
    name: String!
}

type Post implements Node {
    id: ID!
}

type Org implements Named {
    name: String!
}

type Query {
    node(id: ID!): Node
}
