type Query {
    user(id: ID!): User
}

type Mutation {
    renameUser(id: ID!, name: String!): User!
}

type Subscription {
    userRenamed(id: ID!): User!
}

type User {
    id: ID!
    name: String!
    manager: User
}
